services:
  # Фронтенд на Nginx
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.unified
      target: frontend
    ports:
      - "8080:8080"
    depends_on:
      - app
    networks:
      - leaflet-network
    restart: unless-stopped
    # [Sprint 6 / P1-V13] Hardening ported from the now-deprecated
    # docker-compose.prod.yml — resource caps, healthcheck, log rotation.
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
        reservations:
          cpus: '0.1'
          memory: 64M
    healthcheck:
      # [B-014] Use 127.0.0.1 instead of `localhost`: alpine resolves localhost
      # to ::1 (IPv6) first, but nginx binds only to 0.0.0.0 (IPv4). Result was
      # `Connection refused` on every health probe — FailingStreak=1560 (~13ч)
      # despite the public site working fine.
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Бэкенд API
  app:
    build:
      context: .
      dockerfile: Dockerfile.unified
      target: backend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      # Named volume для логов, чтобы избежать проблем с правами доступа
      - app_logs:/app/logs
    # [1A-FU3-S-M2] DB credentials moved to .env / .env.prod (both gitignored).
    # `environment:` would override env_file in docker-compose precedence, so
    # credentials are NOT listed here.
    # Dev:  `cp .env.example .env`, edit. .env.prod absent → silently skipped.
    # Prod: only .env.prod present; .env absent → silently skipped.
    # Both are marked `required: false` so the same compose file works in
    # either mode without symlink/copy gymnastics (requires Docker Compose
    # v2.24+; server runs v2.32+).
    env_file:
      - path: .env
        required: false
      - path: .env.prod
        required: false
    environment:
      # [1A-FU3-S-M2] NODE_ENV also moved to .env so production deploys can
      # actually run in production mode. Hardcoding NODE_ENV=development here
      # caused Helmet CSP to ship with 'unsafe-inline' in script-src on the
      # production server, and Swagger UI was exposed publicly. Dev .env keeps
      # NODE_ENV=development; .env.prod sets NODE_ENV=production.
      - PORT=3000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=infrasafe
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      # leaflet-network — обращение к postgres и frontend
      - leaflet-network
      # uk-network — приём webhook'ов от UK (uk-management-bot → infrasafe-app:3000)
      # external network создаётся при поднятии UK-стека первым.
      - uk-network
    restart: unless-stopped
    # [Sprint 6 / P1-V13] Hardening ported from docker-compose.prod.yml.
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
    healthcheck:
      # [B-014] Same IPv6/IPv4 fix as frontend — use 127.0.0.1 explicitly so
      # we don't depend on the resolver's default v6/v4 preference.
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"

  # [Sprint 4] Redis — shared state for rate-limiter, cache, webhook
  # dedup. Optional service: if commented out OR if REDIS_URL is empty
  # in .env, the app falls back to in-memory implementations and runs
  # single-replica only. Enabling Redis is the prerequisite for
  # horizontal scale-out (--scale app=N).
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes", "--maxmemory", "256mb", "--maxmemory-policy", "allkeys-lru"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - leaflet-network
    restart: unless-stopped

  # База данных PostgreSQL с PostGIS
  postgres:
    image: postgis/postgis:15-3.3

    environment:
      - POSTGRES_PASSWORD=@ppl1c@ti0n
      - POSTGRES_USER=infrasafe_app
      - POSTGRES_DB=infrasafe
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database.sql:/docker-entrypoint-initdb.d/database.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U infrasafe_app -d infrasafe"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - leaflet-network
    restart: unless-stopped
    # [Sprint 6 / P1-V13] Hardening ported from docker-compose.prod.yml.
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1G
        reservations:
          cpus: '1'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # # MQTT брокер Mosquitto
  # mosquitto:
  #   image: eclipse-mosquitto:latest
  #   user: "1000:1000"
  #   ports:
  #     - "1884:1883"
  #   volumes:
  #     - /home/infrasafe/mosquitto/config:/mosquitto/config
  #     - /home/infrasafe/mosquitto/data:/mosquitto/data
  #     - /home/infrasafe/mosquitto/log:/mosquitto/log
  #   environment:
  #     - TZ=Asia/Tashkent
  #   networks:
  #     - leaflet-network
  #   restart: unless-stopped

  # # InfluxDB - база данных временных рядов
  # influxdb:
  #   image: influxdb:alpine
  #   ports:
  #     - "8086:8086"
  #   volumes:
  #     - /home/infrasafe/influxdb/data:/var/lib/influxdb2
  #     - /home/infrasafe/influxdb/conf:/etc/influxdb
  #     - /home/infrasafe/influxdb/engine:/var/lib/influxdb2/engine
  #   environment:
  #     - TZ=Asia/Tashkent
  #   networks:
  #     - leaflet-network
  #   restart: unless-stopped

  # # Grafana - визуализация метрик
  # grafana:
  #   image: grafana/grafana:latest
  #   user: "1000:1000"
  #   volumes:
  #     - /home/infrasafe/grafana/data:/var/lib/grafana
  #     - /home/infrasafe/grafana/conf:/etc/grafana
  #     - /home/infrasafe/grafana/log:/var/log/grafana
  #   ports:
  #     - "3001:3000"  # Изменен порт с 3000 на 3001, т.к. 3000 занят app
  #   environment:
  #     - PUID=1000
  #     - PGID=1000
  #     - TZ=Asia/Tashkent
  #   networks:
  #     - leaflet-network
  #   restart: unless-stopped

  # # Node-RED - визуальное программирование потоков данных
  # nodered:
  #   image: nodered/node-red:latest-minimal
  #   user: "1000:1000"
  #   volumes:
  #     - /home/infrasafe/node-red/data:/data
  #   ports:
  #     - "1880:1880"
  #   environment:
  #     - TZ=Asia/Tashkent
  #   networks:
  #     - leaflet-network
  #   restart: unless-stopped

  # # WireGuard - VPN сервер
  # wireguard:
  #   image: lscr.io/linuxserver/wireguard:latest
  #   privileged: true
  #   volumes:
  #     - /home/infrasafe/wireguard/config:/config
  #   ports:
  #     - "51820:51820/tcp"
  #     - "51820:51820/udp"
  #   environment:
  #     - PUID=1000
  #     - PGID=1000
  #     - TZ=Europe/Moscow
  #     - SERVERURL=95.46.96.105
  #     - SERVERPORT=51820
  #     - PEERS=10
  #     - PEERDNS=1.1.1.1
  #     - INTERNAL_SUBNET=10.13.13.0
  #     - ALLOWEDIPS=0.0.0.0/0
  #     - LOG_CONFS=false
  #   cap_add:
  #     - NET_ADMIN
  #     - SYS_MODULE
  #   sysctls:
  #     - net.ipv4.conf.all.src_valid_mark=1
  #   networks:
  #     - leaflet-network
  #   restart: unless-stopped

  # Nginx Production - Reverse Proxy с SSL
  # Подключён к двум сетям:
  #   - leaflet-network: app, frontend, postgres (InfraSafe stack)
  #   - uk-network: uk-frontend, uk-management-api (UK stack, external)
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Production конфигурация Nginx из проекта
      # ВАЖНО: Файл должен находиться в корне проекта
      # Альтернатива: используйте абсолютный путь /home/infrasafe/nginx-docker/nginx.production.conf
      - ./nginx.production.conf:/etc/nginx/nginx.conf:ro
      # SSL сертификаты — SAN-сертификат на infrasafe.uz + infrasafe.aisolutions.uz
      # Создать на сервере: sudo certbot certonly --webroot -w ~/infrasafe/certbot-webroot
      #                      -d infrasafe.uz -d infrasafe.aisolutions.uz
      # Live-папка certbot по primary-домену (-d первым):
      - /etc/letsencrypt/live/infrasafe.uz/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
      - /etc/letsencrypt/live/infrasafe.uz/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
      - /etc/letsencrypt/live/infrasafe.uz/chain.pem:/etc/nginx/ssl/chain.pem:ro
      # ACME webroot для certbot renew без downtime (см. nginx server { listen 80 })
      - ./certbot-webroot:/var/www/certbot:rw
      # [B-002] HTML pages live in ./frontend-html/. Mounted to a SEPARATE
      # target (/srv/frontend-html), not overlaid on /usr/share/nginx/html —
      # Docker rejects sub-mounts on top of a ro parent. nginx.production.conf
      # routes HTML requests to /srv/frontend-html via a location block.
      # Previously each HTML file was bind-mounted individually, so `git pull`
      # swapped the host inode but Docker kept reading the inode the mount
      # captured at container start — that's why a `docker restart
      # infrasafe-nginx-1` was needed after every HTML deploy. Directory
      # mounts resolve through the dir entry on every request, so fresh
      # content is served the moment `git pull` lands.
      - ./frontend-html:/srv/frontend-html:ro
      # CSS / JS bundles / data — already directory mounts, kept as-is.
      - ./css:/usr/share/nginx/html/css:ro
      - ./public:/usr/share/nginx/html/public:ro
      - ./data:/usr/share/nginx/html/data:ro
    depends_on:
      - app
      - frontend
    networks:
      - leaflet-network
      - uk-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  postgres_data:
  app_logs:
  redis_data:

networks:
  leaflet-network:
    driver: bridge
  # uk-network создаётся UK-стеком (docker-compose.production.yml в репо UK).
  # external: true → этот compose ожидает, что сеть уже существует.
  # Поэтому ПЕРЕД поднятием InfraSafe нужно поднять UK-стек:
  #   docker compose -f ~/uk/docker-compose.production.yml up -d
  uk-network:
    external: true
    name: uk-network