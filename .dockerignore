node_modules
npm-debug.log
logs
*.log
.git
.gitignore
.DS_Store

# [SEC-14/15] Deploy staging for extracted frontend dist — never part of build context
.deploy/

# Secrets — NEVER include in image
.env
.env.*
!.env.example

# [SEC-21] Redis secret config — operator-mounted at runtime, never baked in
redis-config/redis.conf

# Development/test files — not needed in production image
tests/
docs/
coverage/
monitoring/
generator/
frontend-design/
.github/
.worktrees/
*.md
!README.md
docker-compose*.yml
backup-database.sh
swagger_update.js
