# Единый беклог InfraSafe

**Сведён 2026-08-10.** Отменяет как самостоятельные источники:

- `AUDIT_REPORT.md` (6 августа) — ID вида `AR-` / `CO-` / `DE-` / `SE-` / `EN-`
- `docs/audit/sprint-11-backlog.md` (25 июля) — ID вида `M-` / `R2-` / `L-` / `B-` / `AUD-`

`sprint-11-backlog.md` остаётся в репозитории как доказательная база. С
`AUDIT_REPORT.md` иначе: он в `.gitignore` (строка 129) как «вывод скилла
`/audit`, перегенерируется каждый прогон, хранится локально» — то есть **никогда
не был закоммичен**. Августовские находки живут только на машине, где запускался
аудит, и исчезнут вместе с ней.

Поэтому здесь у каждого пункта есть не только ссылка, но и описание сути:
файл должен быть самодостаточным, а не оглавлением к чему-то, чего в репозитории
нет. Обоснования и разбор по-прежнему стоит смотреть в исходных отчётах, если
они под рукой.

## Зачем сведение

Не потому, что списки дублируют друг друга. Августовский аудит читал июльский
заранее, намеренно не переописывал его открытые пункты и взял непересекающееся
пространство идентификаторов — это сказано в его шапке и выдержано. Немногие
совпадения (`AR-8` ↔ `M-2`/`B-003`, `EN-6` ↔ `R2-37`, `AR-3(б)` ↔ `R2-04`/`AUD-008`)
он сам и помечает ссылками на старые ID.

Проблема в другом: **открытое множество не лежит целиком нигде**.

- Июльский список не обновлялся с 25 июля. Шесть его пунктов закрыты августовской
  и последующей работой, и там это не отмечено — список завышает объём.
- Августовский отчёт по построению содержит только новое, то есть заведомо неполон
  как рабочая очередь: двадцать шесть июльских пунктов в него не входят по замыслу.
- Ни один из двух не знает про приоритеты другого. «P1» августовского и «P1»
  июльского — разные шкалы, сравнить их можно только вручную.

Поэтому здесь не третий аудит, а сложение двух с перепроверкой каждой ссылки
и единой шкалой приоритета. Новых находок в этом файле нет — кроме одной
пометки про `sendSuccess`, всплывшей при сверке.

## Аудит 08.09.2026 — сведён сюда же

Третий источник: прогон скилла `/audit` от 8 сентября 2026 (коммит `d602f0f8`),
**26 находок** с ID вида `A-01`…`A-26`.

История повторилась буквально. Преамбула выше объясняет, что августовский
`AUDIT_REPORT.md` лежит в `.gitignore` и потому «никогда не был закоммичен», а его
находки «живут только на машине, где запускался аудит». Ровно это случилось со
сентябрьским прогоном: 26 находок, из них 10 высшей серьёзности, девять дней
пролежали одним файлом на одном ноутбуке и в очередь работ не попали.

Поэтому 17.09.2026 отчёт перенесён в репозиторий целиком —
[docs/audit/2026-09-08-audit.md](audit/2026-09-08-audit.md), как доказательная
база (тем же способом, каким сохранён `sprint-11-backlog.md`), а находки сведены
ниже в общую шкалу.

**Две шкалы не путать.** `P1` у аудита — это СЕРЬЁЗНОСТЬ («высокий риск при
описанных условиях»), `P1` здесь — ОЧЕРЕДНОСТЬ («берём сейчас»). Разнесение по
разделам ниже следует дорожной карте самого отчёта (раздел 8), где этапы
упорядочены по зависимостям, а не по одной лишь серьёзности. Серьёзность по
аудиту указана в каждой строке отдельно.

**Что проверено руками, а не принято на слово.** 17.09.2026 перепроверены по
текущему коду `A-01`, `A-08`, `A-09`, `A-14` — все четыре подтвердились без
поправок. Отчёт точный; остальные находки разбирались по его собственным
доказательствам (раздел 5), а не перепроверялись построчно.

## Легенда

**Приоритет.** `P1` — берём сейчас. `P2` — очередь. `P3` — по касанию.
`P4` — заморожено до наступления условия (вторая реплика, телеметрия, решение владельца).

**Труд.** `XS` — минуты. `S` — один короткий PR. `M` — PR с дизайном. `L` — дробится.

**Проверено.** Все ссылки `файл:строка` перепроверены против `main` на `afd1530`
10 августа 2026. Где адрес изменился с момента заведения — указан новый.
Повторная сверка всех открытых пунктов — 13.08.2026 против `fb92e777`:
4 пункта оказались закрыты (перенесены в «Снято»), 6 адресов съехали (обновлены),
остальные подтверждены грепом.

---

## P1 — берём сейчас

✅ **Волна B выполнена** (PR #185). Ниже — что осталось.

| ID (алиасы) | Что осталось | Доказательство | Труд |
|---|---|---|---|
| ~~**AR-3(б)**, остаток~~ ✅ | ~~4 сырых запроса в пакетных операциях `adminWaterLineController`~~ — **закрыто 12.09.2026**: у модели появились `deleteMany` / `updateStatusMany` / `findConnectedBuildingIds`, в контроллере не осталось ни одного `pool.query`. Проверка связанных зданий сохранена как ветвление admin-пути, а не часть `delete` в модели — иначе запрет навесился бы на обычный `DELETE /api/water-lines/:id`, где его нет; в модель ушёл только запрос. [M-12] переехал внутрь `updateStatusMany`: пока whitelist звал контроллер, он держался на памяти автора следующего пакетного пути. SQL проверен на живом Postgres (`tests/jest/db/waterLineBulk.db.test.js`, 11 тестов, DDL извлекается из канонических `01_init_database.sql` + миграции 040) — мок не показал бы ни потерю половины `OR` по двум FK, ни то, что удаление связанной линии падает на внешнем ключе, то есть что проверка перед удалением превращает 500 в 400 | `grep 'pool.query' src/controllers/admin/` → 0 (было 21) | — |
| ~~**AR-3(в)**~~ ✅ | ~~Новые сущности — только через `createCrudModel`~~ — **закрыто 13.09.2026**: соглашение было записано (шапка фабрики, [R2-09]), не хватало рубежа — `tests/jest/unit/crudFactoryBoundary.test.js`. Сторож НАМЕРЕННО узкий: запрет рукописного SQL во всех моделях противоречил бы области применения фабрики (она покрывает только плоский случай), поэтому падение — ровно в одном случае: все пять методов CRUD написаны руками, а признаков сложности (JOIN / CTE / jsonb / геометрия / ON CONFLICT / оконные функции) в SQL нет. Список исключений ПУСТ — проверка показала, что ни одна из девятнадцати рукописных моделей не является плоско-CRUD'ной, то есть правило описывает сегодняшнее состояние, а не вводится на будущее. Классификатор проверяется на синтетике внутри самого теста (сканер исходников умеет протухнуть молча) и мутацией: добавленная в `src/models/` плоская модель роняет сборку | — | — |

### Аудит: доступ и деплой (этапы 1–2 дорожной карты отчёта)

Взято в P1 не по одной серьёзности, а потому что это либо обход действующей
защиты, либо гарантии, на которые уже опирается выкатка. Оценка серьёзности — из
отчёта; «проверено» — перепроверено вручную 17.09.2026.

| ID (серьёзность) | Что не так | Доказательство | Труд |
|---|---|---|---|
| ~~**A-01**~~ ✅ (серьёзность P1) | ~~**Второй фактор обходится одним паролем.**~~ — **закрыто 17.09.2026** (PR #226, на обеих площадках в `f12fb5e7`): `generateSetup` отвечает 409 при `totp_enabled`, а `User.setTotpSecret` пишет условным UPDATE `... AND totp_enabled = false` с проверкой `rowCount` — окно между чтением состояния и записью закрыто в самой БД, а не только в сервисе. Закрытие обхода отняло у потерявшего аутентификатор последний путь восстановления, поэтому тем же PR добавлен `src/cli/reset-2fa.js`: сброс идёт из контейнера, снимает секрет/флаг/коды И отзывает сессии одним запросом (иначе угнанная сессия пережила бы сброс) | гонка проверена на живом Postgres — `tests/jest/db/totpSecretGuard.db.test.js` | — |
| ~~**A-16**, часть A-01~~ ✅ (P2) | ~~`totpService.test.js:243` закрепляет замену включённого TOTP как ПРАВИЛЬНОЕ поведение~~ — **закрыто 17.09.2026** (PR #226): тест инвертирован в проверку отказа. Его прежнее оправдание («после admin reset 2FA») описывало НЕСУЩЕСТВУЮЩИЙ сценарий — админского сброса в коде не было вовсе, а `totpService.disable` админам прямо запрещает снимать 2FA. Теперь такой путь есть, но он проходит через CLI, а не через HTTP | — | — |
| ~~**A-09**~~ ✅ (P1) | ~~**Обещанный откат не происходит.**~~ — **закрыто 17.09.2026** (PR #227): `trap on_exit EXIT` рядом с ERR-ловушкой плюс флаг `DEPLOY_OK`, снимаемый только в точке успеха, — ловит и явный `exit`, и ветку `\|\|`-списка, и любой будущий выход. Обе формы отказа воспроизведены руками: со старой схемой откат НЕ выполнялся, с новой выполняется, успешный путь не тронут. Рубеж в `deployWiring.test.js` сначала оказался фиктивным — закомментированная строка `# trap on_exit EXIT` удовлетворяла регулярке; проверки переведены на код без строк-комментариев, три мутации ловятся | `update-production.sh`, `tests/jest/unit/deployWiring.test.js` | — |
| ~~**A-14**~~ ✅ (P2) | ~~публикация образа идёт ПАРАЛЛЕЛЬНО проверкам~~ — **закрыто 17.09.2026** (PR #227): у job `docker-image` появился `needs: [lint, test, audit, gitleaks]`. Имя job'а НЕ менялось намеренно — required-check в защите ветки матчится по строке `Docker image (SEC-14/15 immutable app)`, переименование сломало бы мерж всех открытых PR. Видно в деле: на прогоне `main` `f12fb5e7` сборка образа стартовала только после четырёх зелёных проверок | `.github/workflows/ci.yml` | — |
| ~~**A-13**~~ ✅ (P2) | ~~`nginx -t` вызывается без `-c` и проверяет не тот конфиг~~ — **закрыто 17.09.2026** (PR #227): путь берётся из заголовка процесса ЖИВОГО мастера, поэтому проверка не может разойтись с тем, что исполняется; при запуске без `-c` — запасной `nginx -t`. Доказано на живом контейнере: без `-c` заведомо битый конфиг даёт `exit 0`, с `-c` — `exit 1`, то есть шаг 6b не мог поймать поломку в принципе. Первая версия разбора молча возвращала пусто — nginx переписывает argv в один заголовок, и NUL-разбиение `/proc/1/cmdline` там не работает | `update-production.sh` | — |
| ~~**A-02**~~ ✅ (P1) | ~~middleware отклоняет израсходованный refresh ДО сервиса, отзыв сессий недостижим~~ — **закрыто 17.09.2026** (PR #226, миграция 044): у `token_blacklist` появилась колонка `reason` (expand-only, nullable + CHECK — старый образ её игнорирует, откат безопасен), ротация пишет `'rotation'`, логаут `'logout'`, temp-token свой; middleware больше не предрешает исход, решение принимает `_handleRefreshReuse`. Строки ДО 044 (`reason IS NULL`) сохраняют прежнюю логику по времени — намеренно: ложный разлогин восстанавливается входом, пропущенная кража — нет | `tests/jest/db/tokenBlacklistReason.db.test.js` | — |
| ~~**A-11**~~ ✅ (P2) | ~~**Коды восстановления не работают**, вход завершается 500-й~~ — **закрыто 17.09.2026** (PR #226): формат выбирает ветку ДО вызова otplib, сам вызов обёрнут. Регрессия идёт против НАСТОЯЩЕЙ библиотеки (`totpService.realOtplib.test.js`) — корневой `__mocks__/otplib.js` подставляется во все юнит-тесты автоматически и возвращал `{valid:false}` там, где живая otplib бросает, поэтому прежние тесты доказывали ровно ничего. Сам мок заодно приведён к контракту otplib 13.4.1, снятому с библиотеки напрямую | `tests/jest/unit/totpService.realOtplib.test.js` | — |
| ~~**A-08**~~ ✅ (P1) | ~~**Карта врёт про горячую воду.**~~ — **закрыто 17.09.2026** (PR #228): вхождений оказалось ТРИ, а не одно — кроме классификации, попап всегда писал «ГВС: Не подключено», а в промышленную панель уходило `hasHotWater=true` для всех домов. Логика вынесена в `public/utils/buildingStatus.js` и покрыта 14 тестами: внутри 2263-строчного `script.js` проверить её было нечем, и ровно в таком месте подмена имени поля живёт годами. Legacy `hot_water` НЕ читается сознательно — поддержка обоих имён сохранила бы ту самую двусмысленность. **Латентно на сегодняшних данных**: `has_hot_water=false` у всех 30 зданий profk и всех 8 на infrasafe.uz, эффект появится с первой галкой в админке | `public/utils/buildingStatus.js`, `tests/jest/unit/buildingStatus.test.js` | — |
| ~~**A-12**~~ ✅ (P2) | ~~подпись принимается в нескольких написаниях, а ключ дедупа считается по исходной строке~~ — **закрыто 17.09.2026** (PR #231): канонизация вынесена в `src/utils/hmacSignature.js` и зовётся обоими входами — телеметрией и вебхуками УК. Тонкость, которую стоит помнить: проверка длины БЫЛА, но сравнивала длины ДЕКОДИРОВАННЫХ буферов, а `Buffer.from(sig,'hex')` обрывает разбор на первом не-hex символе молча — поэтому `sig+'ZZ'` давал те же 32 байта и другой ключ дедупа. Верхний регистр НЕ запрещён намеренно: отправители нам не подконтрольны, hex регистронезависим, и отказ по регистру был бы поломкой совместимости ради чистоты — задача была свести написания к одному ключу (закреплено тестом «верхний регистр по-прежнему принимается») | `tests/jest/unit/hmacSignature.test.js`, плюс по 3 и 2 теста повтора на входах | — |

**Состояние всей волны на 18.09.2026: закрыта ПОЛНОСТЬЮ — 26 из 26 — и
выкачена на обе площадки.** Коммит `0027672d`, образ
`ghcr.io/a-afanasyev/infrasafe-app@sha256:df6cf295` (digest сверен, совпал на
profk и .105). Миграций в этой партии не было — схема на обеих уже стояла на
044; `.env` не правился: единственная новая переменная
`UK_INTENT_RECONCILE_ENABLED` имеет умолчание. Сверяющий проход `A-03`
поднялся на обеих площадках без ошибок; тиков в логе не видно, и это верно —
на profk открытых алертов нет вовсе, на .105 все 26 в `resolved`.

**Осталось операционное, не кодовое:** содержательная репетиция отката на .105
(нужно увести площадку на релиз назад, чтобы откату было что восстанавливать) —
см. «Незакрытый остаток» ниже.

**Состояние блока на 17.09.2026: закрыт целиком и выкачен.** PR #226 (доступ),
#227 (деплой), #228 (карта) уехали на обе площадки в `f12fb5e7` — миграция 044
применена до подмены образа, штатным порядком. Следом PR #230 (`A-10`), #231
(`A-12`) и групповой #229 (**morgan 1.12.1, CVE-2026-87859** — боевая
зависимость) выкачены в `14699a5e`, образ `sha256:894b3b99` на обеих площадках.

`A-12` проверен на ЖИВОМ образе, а не только тестами: проба в контейнере profk
дала `uppercaseReplay=false`, `suffixZZReplay=false` при `uppercaseFresh=true` —
у аудита в этом месте стояло `true/true`. Тонкость, ради которой стоит помнить
находку: проверка длины БЫЛА и раньше, но сравнивала длины ДЕКОДИРОВАННЫХ
буферов, а `Buffer.from(sig,'hex')` обрывает разбор на первом не-hex символе
молча.

**Найдено при живой проверке, в аудите этого нет.** Проверяя ротацию refresh
после `A-02`, я увидела, что два токена, выпущенные в одну секунду, совпадают
байт в байт: `iat` секундной точности, прочие claim'ы те же — значит и хеш в
`token_blacklist` один, и `ON CONFLICT` молча схлопывает две разные сессии в одну
строку. Сначала я списала это на артефакт разбора и ошиблась. Закрыто там же
(PR #226): access-, refresh- и temp-токены получают `jti` (`crypto.randomUUID()`),
рубеж — `tests/jest/unit/tokenUniqueness.test.js`.

**Репетиция отката, .105, 17.09.2026 — и что она нашла.** Первое за всё время
исполнение отката на живой площадке. Ловушка на позднем отказе СРАБОТАЛА: шаг 8
упал через явный `exit 1`, откат прошёл, площадка цела. Ради этого A-09 и
делался, и до репетиции это было обещанием, а не фактом.

Тут же вскрылся дефект В САМОЙ ПРАВКЕ, которого не видел ни один статический
рубеж: **после отката скрипт возвращал НОЛЬ**. Человек читает «rollback complete»
и всё понимает, а `&&`-цепочка или cron читают успех. Две причины, обе из правки
A-09: (1) `on_exit` перед вызовом отката выполняет
`if [ "$rc" = 0 ]; then exit 0; fi`, а у составного `if` с ложным условием и без
`else` статус равен НУЛЮ — он и попадал в `local rc=$?`; (2) ловушек было две, и
на отказе простой команды под `set -e` откат исполнялся ДВАЖДЫ, теряя код уже на
первом проходе (замерено: с обеими ловушками `1` вместо настоящего `7`, с одной
EXIT — `7`). Закрыто PR #233: код передаётся параметром, ловушка одна, проверка
стала ПОВЕДЕНЧЕСКОЙ — настоящий блок ловушек запускается bash'ем с заглушками
вместо docker/git.

Побочное наблюдение, полезное для следующей репетиции: периметр отдаёт `200` на
неизвестный путь (SPA-фолбэк), поэтому подстановка выдуманного URL в
`EDGE_HEALTH_URL` НЕ роняет шаг 8 — годится только адрес, где никто не слушает
(`http://127.0.0.1:1/health`).

**Чего репетиция НЕ доказала.** Она была сокращённой: команду, переводящую
площадку на релиз назад, заблокировал классификатор, поэтому `PREV == TARGET` и
содержательная часть восстановления — возврат HEAD, статики и образа, то есть
вся правка `A-10` — не проверялась. Подтверждены только срабатывание ловушки и
чистое завершение отката. Полная репетиция требует одного разрешённого шага:
перевести `/home/infrasafe/infrasafe` на предыдущий коммит и образ, чтобы откату
было что восстанавливать.

Оговорка про порядок остаётся: правка скрипта вступает в силу со СЛЕДУЮЩЕЙ
выкатки — та, что её привозит, идёт ещё старым скриптом с хоста.

**Почему `AR-3(б)` и «Новое №8» шли одним PR.** Перевод контроллеров на модели
заставляет модели бросать типизированные 4xx — а действующий catch их проглатывал
и отдавал 500. Сделать перенос раньше правки catch значило бы временно **ухудшить**
поведение: проверка появилась бы, но снаружи стала неотличима от аварии.

---

## P2 — очередь

### Аудит: сохранность данных, показания, интеграция, воспроизводимость (этапы 3–5)

Серьёзность по отчёту у большинства этих находок P1, но каждая требует широкой
проверки сценария (DOM, SQL, инъекция отказов), поэтому они идут после этапа
доступа и деплоя — порядок из дорожной карты отчёта, раздел 8.

| ID (серьёзность) | Что не так | Доказательство | Труд |
|---|---|---|---|
| ~~**A-06**~~ ✅ (P1) | **Закрыто 17.09.2026** (PR #234): бэкенд переведён на частичное обновление (`buildUpdateQuery`), различие «не передано» / «сброшено» стало выразимым, пустое тело — 400. На фронте справочники запрашиваются целиком (`limit=200`), текущая связь гарантируется отдельной опцией, а три состояния поля вынесены в `public/utils/buildingFormPayload.js`. Важная поправка к формулировке находки: страница-2 сегодня не набирается (справочники почти пусты), но ВТОРАЯ половина от числа строк не зависит — достаточно, чтобы `loadFormData` не отработал, и одно сохранение стирало все восемь связей разом. SQL проверен на живом Postgres; показательно, что ни один из 3264 юнит-тестов на смену запроса не отреагировал. ~~**Редактирование здания стирает связи.** Справочник в admin-форме ограничен первой страницей из 10 объектов. Если текущий FK за её пределами, `option` для него нет, `select` оказывается пустым, submit поле пропускает, а fixed UPDATE записывает `NULL` — смена НАЗВАНИЯ здания удаляет инфраструктурную связь.~~ | `tests/jest/db/buildingPartialUpdate.db.test.js`, `tests/jest/unit/buildingFormPayload.test.js` | — |
| ~~**A-07**~~ ✅ (P1) | ~~**Карта не отличает «данных нет» от «всё в норме».**~~ — **закрыто 17.09.2026** (PR #235). Находка оказалась резче, чем описана, и НЕ латентной: на infrasafe.uz в день правки оба контроллера `offline`, последние метрики от 13 и 16 ИЮНЯ (92 дня), и карта по ним показывала «Yangi Olmazor 14V» нормой (220/220/220), а «Yangi Olmazor 13V» — ДЕЙСТВУЮЩЕЙ протечкой с мигающим маркером. Дефект бьёт в обе стороны: ложное спокойствие и ложная тревога. Проверка актуальности встала ПЕРЕД оценкой показаний; порог 10 минут взят тот же, по которому планировщик помечает контроллер offline (`controllerService.statusTimeout`) — расхождение дало бы окно, где карта и статус контроллера противоречат друг другу; новый статус `stale` получил группу в панели, окраску кластера и строку в popup'е. Там же две смежные части: признак «телеметрия есть» смотрел только на `electricity_ph1` (пакет с одним датчиком протечки до классификации не доходил), а индикатор свежести брал время HTTP-опроса и писал «только что» при трёхмесячных данных | `public/utils/buildingStatus.js`, `tests/jest/unit/buildingStatus.test.js` | — |
| ~~**A-05**~~ ✅ (P1) | ~~**Короткий CRITICAL съедает устойчивый WARNING.**~~ — **закрыто 17.09.2026** (PR #236): классификатор отдаёт СПИСОК уровней в окне, а `_selectVoltageSeverity` берёт первый, ПРОШЕДШИЙ свой gate. Ключевое решение: гейт спрашивается ЯВНО, а не выводится из `null` от `createAlert` — тот отказывает и по кулдауну, и по дедупу, а понижать уровень можно только из-за гейта (иначе понижение стало бы вторым алертом на ту же аварию). Отсутствие правила трактуется как в `createAlert` (гейтов нет), иначе селектор стал бы строже и молча душил бы типы без политики. Правило выбранного уровня уходит в `createAlert` снимком — между проверкой и вставкой его могли отключить | `tests/jest/unit/alertService.severitySelection.test.js`; мутации: выбор без гейта роняет 3 теста, взведение кулдауна при отказе — 1 | — |
| ~~**A-03**~~ ✅ (P1) | **Закрыто полностью 18.09.2026.** Первая половина (PR #237, 17.09) — окно между записью ARM и постановкой в outbox: обе идут одной транзакцией. Вторая половина — окно РАНЬШЕ этой пары: алерт коммитится, и только потом `sendNotifications` эмитит `ALERT_CREATED`; падение процесса между ними оставляло открытую аварию без заявки НАВСЕГДА (повторный алерт душит дедуп, drain доставляет существующие строки очереди, но не восстанавливает отсутствующие). Сделан сверяющий проход `alertIntentReconciler` — синглтон с межрепличным advisory-локом, как у drain-воркера. **Риск, который и был предметом дизайна, решён без миграции:** наивный проход на здании без `external_id` (УК их не присылает) или на типе без правила эскалации звал бы форвардер вечно — строка намерения не появится никогда, условие отбора не изменится. Оба состояния распознаются ДО вызова и помечаются терминально в `data.uk_reconcile.skipped_reason`; всё остальное считается попыткой с потолком 3. Отметка живёт в JSONB рядом с `notification_failures` — колонки и миграции не потребовалось. Успех нельзя взять из возвращаемого значения (`sendAlertToUK` по контракту не бросает и возвращает undefined) — проход перечитывает строку намерения после вызова. Флаг `UK_INTENT_RECONCILE_ENABLED` по умолчанию ВКЛЮЧЁН: выключенный по умолчанию механизм починки — ровно то, чем однажды стал AUD-001; безопасность дают гейты (интеграция выключена → пустой тик, окно 24ч, пачка ≤5). **Честное ограничение:** метрический контекст FE-119 в БД не хранится, восстановленная заявка уходит без него. ~~INSERT алерта, запись ARM и enqueue в outbox не атомарны.~~ | `tests/jest/db/alertIntentAtomic.db.test.js`, `tests/jest/db/alertIntentReconcile.db.test.js` (отбор — в SQL, мутации сняты: снятие фильтра `skipped_reason` и замена `jsonb_set` на перезапись `data` роняют ровно свои тесты), `tests/jest/unit/alertIntentReconciler.test.js` (включая рубеж «проход подключён к серверу» — против повторения AUD-001) | M |
| ~~**A-04**~~ ✅ (P1) | ~~ARM помечается `resolved` ДО асинхронного `resolveAlert`, и reconcile после этого молчит — алерт навсегда `active`~~ — **закрыто 17.09.2026** (PR #238): признаком незавершённости стал сам алерт, и reconcile повторяет emit, если УК считает заявку терминальной, а алерт остался открытым. Условие СУЖЕНО намеренно: прежний no-op ставился review-фиксом 23.07 против конкретного шума (повтор по давно закрытому алерту каждый цикл), и этот шум не вернулся — на сохранение прежнего поведения есть отдельный тест. Осознанный компромисс: ошибка чтения статуса алерта трактуется как «не повторять» (иначе шторм повторов), но пишется предупреждение — при устойчиво сбойном чтении дыра остаётся открытой, зато видимой | `tests/jest/unit/ukIntegrationServiceTest.test.js`; мутации: безусловный no-op роняет 2 теста, безусловный повтор — 3 | — |
| ~~**A-10**~~ ✅ (P1) | ~~откат возвращает статику только в worktree, HEAD остаётся новым~~ — **закрыто 17.09.2026** (PR #230): откат идёт `git reset --hard "$PREV_COMMIT"` — возвращаются ВСЕ отслеживаемые файлы (включая `nginx-config/`, которого в прежних четырёх каталогах не было вовсе) вместе с HEAD. Воспроизведено на игрушечном репозитории: со старым откатом конфиг периметра оставался новым, а повторное слияние того же SHA печатало «Already up to date» и не восстанавливало ничего. Узкий restore остался запасным путём для случая, когда дерево было грязным ДО выкатки (там `reset --hard` затёр бы правки оператора) — с громким предупреждением, что HEAD остался впереди. Проба чистоты игнорирует untracked-файлы: на хостах их десятки | `update-production.sh`, `tests/jest/unit/deployWiring.test.js` (мутации ловятся) | — |
| ~~**A-15**~~ ✅ (P2) | ~~Quick start поднимает init до миграции 017 без `migrate up`~~ — **закрыто 17.09.2026** (PR #244): в quick start добавлен обязательный шаг с готовой командой и проверкой `status`, и сказано, ПОЧЕМУ он не автоматизирован (раннер исполняет миграции из закреплённого коммита, а не из рабочего дерева — молча применять схему при каждом `up` нельзя). Рубеж сторожит не текст, а факт наличия шага, и вдобавок сверяет, что init-схема ДЕЙСТВИТЕЛЬНО отстаёт от каталога миграций: если однажды перестанет, предупреждение в README станет ложью, и тест об этом скажет | `tests/jest/unit/bootstrapDocs.test.js`; мутация — удаление шага из README — роняет 2 теста | — |
| ~~**A-16**~~ ✅ (P2) | Тесты закрепляют небезопасные контракты. ~~Замена включённого TOTP~~, ~~otplib подменена именно в recovery-тесте~~ и ~~деплой проверяется регуляркой~~ — закрыты 17.09.2026 (PR #226/#227): тест инвертирован, регрессия на коды восстановления идёт против настоящей библиотеки, рубежи деплоя переведены на код без строк-комментариев (закомментированная строка удовлетворяла прежней регулярке). ~~No-op по resolved ARM без проверки алерта~~ — закрыт вместе с `A-04` (PR #238): у того теста предпосылка была НЕЯВНОЙ (он проходил на протёкшем моке `db.query`), теперь в нём прямо сказано, что «тишина» верна только для закрытого алерта, а открытый проверяется отдельным тестом. Покрытие 92% этих сквозных свойств не проверяет | `tests/jest/unit/totpService.test.js:243,394`, `ukIntegrationServiceTest.test.js:790`, `deployWiring.test.js:208` | M |
| ~~**A-17**~~ ✅ (P2) | **Закрыто 17.09.2026** (PR #240). Находка перестала быть условной: условие достигается одной командой — положила в `.claude/worktrees/probe` копию package.json, копию `__mocks__/otplib.js` и падающий тест, и получила дубликат ручного мока, Haste-коллизию и ЧУЖОЙ тест другой ветки в прогоне. Дубликат мока здесь хуже коллизии: неизвестно, какой из двух подставится, — а на этом моке держится вся проверка 2FA. Одного `testPathIgnorePatterns` мало (он про тесты), нужен и `modulePathIgnorePatterns` (карта модулей); прописано в обоих конфигах, включая E2E с собственным rootDir. ~~Jest обходит вложенные worktree: в checkout аудита были duplicate mock otplib и Haste-коллизии, прогон захватил тест другой ветки. **Условно:** проявляется, только когда в `.claude/worktrees` реально лежит дерево — 17.09 каталог пуст, десяток полных прогонов чистые.~~ | `tests/jest/unit/jestWorktreeIsolation.test.js` | — |
| ~~**A-22**~~ ✅ (P2) | ~~`npm audit` в CI проверяет только основной пакет~~ — **закрыто 17.09.2026** (PR #242): отдельный шаг с `working-directory: generator` и ТЕМ ЖЕ порогом, что у основного пакета (расхождение порогов — это два разных правила, о которых никто не помнит; на совпадение есть тест). Три moderate в `qs` (через express и body-parser) вычищены `npm audit fix` в дереве генератора — их не видел ни один прогон CI, то есть «CI зелёный» про генератор не говорил ничего | `tests/jest/unit/deployWiring.test.js`; мутации: снятие шага роняет 2 теста, расхождение порогов — 1 | — |
| ~~**A-23**~~ ✅ (P2) | ~~Генератор не авторизуется на GET `buildings-metrics`, анонимный DTO без `controller_id`, фильтр даёт пустой список, и генератор рапортует успех~~ — **закрыто 17.09.2026** (PR #243). Проверено на живом стенде: анонимная выдача — 31 строка БЕЗ ключа `controller_id` (есть только `has_controller`), то есть прежний фильтр гарантированно давал пусто. Сделано: запрос идёт с `Authorization` (проверено echo-сервером — заголовок реально уходит); анонимная проекция отличается от «зданий нет» и даёт НАЗВАННУЮ ошибку, а не тишину; `runOnce` возвращает сводку с предупреждением, и эндпоинт его отдаёт — пустой прогон больше не выглядит успехом; логин, встретив `requires2FA`, говорит об этом прямо и советует `API_STATIC_TOKEN`, вместо безликого «не удалось получить токен» | проверки — пробниками против живого стенда и локального echo-сервера (у генератора своё ESM-дерево, в jest-сьюту он не подключён) | — |
| ~~**A-24**~~ ✅ (P2) | ~~Named volume создаёт КАТАЛОГ на пути `generator-data.json`~~ — **закрыто 17.09.2026** (PR #241): том монтируется на каталог `/app/data`, файл лежит внутри. Проверено запуском настоящего store на ОБЕИХ формах монтирования: на каталоге `saved: true`, на старой схеме `EISDIR` на чтении и записи. Правка шире пути: пустые `catch` убраны (именно тишина превращала «том смонтирован не туда» в «настройки пропали»), запись стала атомарной, `saveConfig` возвращает признак успеха, и API отвечает 500 вместо ложного успеха. Рубеж проверяет ОБЕ половины — и compose, и store, — иначе том оказался бы на каталоге, а файл по-прежнему вне тома | `tests/jest/unit/generatorVolumeMount.test.js` | — |


### ✅ Со сроком до телеметрии — закрыто волной E (PR #188)

ADR #138 слит, календарь определён — пункты взяты.

| ID | Итог |
|---|---|
| **AR-21** | Планировщик заведён (`controllerStatusScheduler`, интервал 120 с при окне 600 с). Форма — как у трёх существующих воркеров: синглтон, advisory-лок между репликами, env-гейт, тик не бросает |
| **Счётчик представления** | Миграция 042: домен `controllers.status` закреплён CHECK'ом, `active_controllers_count` считает `'online'` вместо несуществующего `'active'`, сид исправлен |
| **AR-17** | **Снят как не-проблема, с замером.** Подробности ниже |

**Почему AR-17 снят.** Формулировка «агрегирующий запрос на каждый сэмпл»
звучит тревожно, но запрос идёт по индексу `idx_metrics_ctrl_ts`
(`controller_id, timestamp DESC`) в окне 600 секунд — это около десяти строк.
Замер `EXPLAIN (ANALYZE, BUFFERS)` на живой базе: **0.18 мс, один буфер**.
При целевых 200 контроллерах × 1/мин это 3.3 вызова в секунду, то есть порядка
0.06 % одного ядра.

Важнее, что очевидная «оптимизация» здесь вредна: взводить cooldown на здоровом
сэмпле прямо запрещено — так маскируется persistence-гейт, и этот урок уже
оплачен коммитом `e15436f`. Писать код ради 0.18 мс, рискуя вернуть закрытый
дефект, — плохая сделка.

### Валидация и контракты

✅ **Волна C выполнена** (PR #186): `CO-7`, `L-6`, `L-7` закрыты целиком, `AR-11` — наполовину.

| ID | Что осталось | Доказательство | Труд |
|---|---|---|---|
| ~~**AR-11**, остаток~~ ✅ | ~~Общего хелпера транзакций нет: пять мест пишут `BEGIN`/`COMMIT` вручную~~ — **закрыто 12.08.2026**: `db.withTransaction(fn, {client?, context?})` в двух режимах — своё соединение (connect+release) и чужое (воркер верификации держит клиент под advisory-локом и освобождает двумя кадрами выше; освободить его внутри значило бы выдернуть соединение из-под чужого кадра). Переведены все пять мест. Границу сторожит архитектурный тест: `query('BEGIN')` вне `config/database.js` роняет сборку | `grep "query('BEGIN')" src/` → только сам хелпер | — |

### Валидация и контракты

✅ **Волна C выполнена** (PR #186): `CO-7`, `L-6`, `L-7` закрыты целиком, `AR-11` — наполовину.

| ID | Что осталось | Доказательство | Труд |
|---|---|---|---|
| ~~**AR-11**, остаток~~ ✅ | ~~Общего хелпера транзакций нет: пять мест пишут `BEGIN`/`COMMIT` вручную~~ — **закрыто 12.08.2026**: `db.withTransaction(fn, {client?, context?})` в двух режимах — своё соединение (connect+release) и чужое (воркер верификации держит клиент под advisory-локом и освобождает двумя кадрами выше; освободить его внутри значило бы выдернуть соединение из-под чужого кадра). Переведены все пять мест. Границу сторожит архитектурный тест: `query('BEGIN')` вне `config/database.js` роняет сборку | `grep "query('BEGIN')" src/` → только сам хелпер | — |

### ✅ Конфигурация — закрыто волной D (PR #187)

`AR-9`, `M-7`/`M-11` и `EN-6` закрыты одним узлом: `env.js` отвечал только на
вопрос «переменная задана?», и `DB_POOL_MAX=twenty` давал `NaN`, с которым
приложение спокойно стартовало.

Теперь грамматика значений живёт в `config/envSchema.js` (целые с границами,
булевы, перечисления, схемы URL), `REDIS_URL` обязателен в production, а
тихая деградация на per-process память публикуется гейджем
`infrasafe_redis_degraded`. Правило тревоги на него — за `profk-observability`.

### Тестовая инфраструктура

| ID | Что | Доказательство | Труд |
|---|---|---|---|
| ~~**R2-14**~~ ✅ | ~~E2E так и не на PR — только `schedule` и `workflow_dispatch`~~ — **закрыто 12.08.2026**: добавлен триггер `pull_request` + `concurrency` (новый пуш в PR отменяет предыдущий прогон, ночные и ручные — нет). Исходный довод «медленно и тяжело по докеру» не подтвердился: 2–3 минуты по восьми ночным запускам, дешевле job'а `Docker image`, который и так собирает образ на каждом PR. Цена отсутствия измерена ровно один раз: 12.08 правка `M-5` уронила E2E, merge в 04:31 UTC, ночной прогон поймал в 04:43 — на PR было бы видно до мержа | — | — |
| ~~**FLAKE-1**~~ ✅ | ~~Полный прогон изредка валит лимитерный сьют по таймауту 5 с~~ — **закрыто 14.08.2026**: причина — десятки последовательных supertest-запросов под поделённым между jest-воркерами CPU; дефолтных 5 с изредка не хватало. Обоим цикловым тестам дан явный таймаут 15 с, потолок 429-цикла срезан с 200 запросов до квоты+запас (70) | `buildingMetricsRoute.limit.test.js` | — |
| ~~**R2-24**~~ ✅ | ~~Прямые тесты `AccountLockout` есть, но БД в них замокана~~ — **закрыто 12.08.2026**: `tests/jest/db/accountLockout.db.test.js` (13 тестов) гоняет модель через настоящий Postgres; в CI поднят шаг `npm run test:db` на уже существовавшем, но простаивавшем сервисе `postgres`. Suite не пропускается при недоступной БД (пропуск = ложный зелёный) и проверен мутациями: порча единицы интервала и отключение CTE-зеркала обе ловятся | — | — |
| **STAGING-VM** = `R2-15 Phase B` (+ **Новое №4**) | Staging из репозитория собрать нельзя: `nginx.staging.conf` нет, `bootstrap-staging.sh` нет. Тест проверяет только *ссылку* на файл → CI зелёный при неработоспособном окружении. Пока его нет, любой деплой идёт сразу в два прода | `ls nginx-config/` → dev/production/profk; `stagingComposeOverride.test.js:28` | M + решение |

### Сессионный слой

| ID | Что | Доказательство | Труд |
|---|---|---|---|
| ~~**M-5**~~ ✅ | ~~`/auth/disable-2fa` — подбор пароля мимо per-account lockout~~ — **закрыто 12.08.2026**: `verifyPasswordOnly` заменён на `verifyReauthPassword`, промах считается в блокировке по ключу `username` + переносится на `users.account_locked_until`. Заодно закрыт **второй такой же оракул** рядом — `/auth/change-password`, где проверка текущего пароля тоже не считалась (в бэклоге он не значился) | — | — |
| ~~**M-6**~~ ✅ | ~~Реплей refresh отдаёт 401, но отзыва всех сессий нет~~ — **закрыто 12.08.2026**: рубеж `users.sessions_revoked_at` (миграция 043) + максимум с `password_changed_at` в `_isIssuedBeforeCutoff` — действует сразу во всех трёх ветках middleware, в refresh и в 2FA-temp-токенах. Реплей refresh отзывает всё семейство, кроме повтора в окне добросовестной гонки (10 с). Плюс `POST /auth/revoke-sessions`. Взят timestamp, а не `token_version`: механизм cutoff уже был, а новый claim потребовал бы ответа «что делать с уже выданными токенами без него» | — | — |
| ~~**M-4**~~ ✅ | ~~`tempToken`, TOTP-секрет и recovery-коды в теле ответа~~ — **закрыто 12.08.2026**. Пункт оказался ДВУМЯ разными задачами. (1) `tempToken` — то же упущение, что у access/refresh: уехал в HttpOnly-куку `temp_token`, из тела убран, middleware читает куку (тело — запасной путь на выкладку), CSRF-guard расширен на три шага 2FA. (2) Секрет и коды — НЕ дефект того же рода: их обязан прочитать человек, в куке они бессмысленны. Осмысленное сделано другое — коды переехали с `setup-2fa` на `confirm-2fa` (раньше выдавались ДО подтверждения и, из-за идемпотентности setup, повторялись при каждом обновлении страницы); добавлен отдельный шаг «я сохранил коды» в обоих фронтендах. Проверено в браузере на обеих точках входа + E2E 11/65 | — | — |
| ~~**M-4-contract**~~ ✅ | ~~Убрать `recoveryCodes` из ответа `setup-2fa`~~ — **закрыто 14.08.2026**: поле снято из ответа `setup-2fa` И из возврата `generateSetup` (до confirm'а plaintext живёт только в кэше — контроллеру нечего перепробросить). Ожидание «когда бандл разойдётся» оказалось перестраховкой: JS отдаётся с `Cache-Control: no-cache` (хотфикс 27.05), окно закрылось сразу после выкатки 12.08. Рубеж — `setup2faContract.test.js`: контроллер не пробрасывает поле, даже если сервис его вернёт | — | — |

> **Важно про охват.** Августовский аудит объявил JWT и криптографию чистыми, и по
> существу прав: проверены алгоритм, `issuer`/`audience`, различение трёх типов
> токенов, `timingSafeEqual` во всех сравнениях подписей. Но проверялась стойкость
> **токена**, а не жизненный цикл **сессии**. Эти три пункта попадают ровно в
> промежуток, который строка «JWT — чисто» невольно маскирует.

### Найдено волной B — требует решения

| Что | Подробности |
|---|---|
| **Поверхность записи трансформатора** | Конструктор `Transformer` читает пять колонок из AUD-039 (`address`, `voltage_primary`, `voltage_secondary`, `maintenance_contact`, `notes`), но **ни один** путь записи их не разрешает — ни admin, ни обычный. То есть колонки заполнены миграцией 036 и с тех пор доступны только на чтение. Либо добавить их в `TRANSFORMER_UPDATE_COLUMNS`, либо признать read-only осознанно. Расширять белый список молча, под видом рефактора, я не стал |
| **`geom` в ответах API** | Все пять инфраструктурных таблиц имеют колонку `geom` (бинарь PostGIS), и старые admin-ответы отдавали её наружу через `SELECT *`. После волны B она уходит из ответов детальных карточек. В листингах (`selectSql: wl.*`) остаётся. Стоит решить, нужна ли она там вообще |

### ✅ Решения приняты 2026-08-11

| ID | Решение | Обоснование |
|---|---|---|
| **M-20** | **Принято как есть.** Аноним получает адреса и координаты зданий | Владелец: «точные координаты не проблема, они доступны в гугле». Записи об осознанном принятии не было — теперь есть, и следующий аудит не заведёт пункт заново |
| **STAGING-VM** = `R2-15 Phase B` | **Отложено.** Промежуточной площадки нет, деплой идёт сразу в два прода | Решение владельца. Это не умолчание, а выбор: цена VM выше текущего риска. Пересмотреть, если появится второй разработчик или деплои начнут ломать прод чаще раза в квартал |
| **DE-6** | **Оставляем curl-only.** UI для `alert_suppressions` не заводим | Заглушка алертов — редкий операторский манёвр, а не повседневная кнопка. UI для неё стоит дороже, чем экономит, и добавляет способ случайно заглушить аварию. Опасность была не в отсутствии UI, а в том, что решение нигде не записано: эндпоинты существуют, о них никто не знает. Теперь записано |
| **`geom` в листингах** | **Оставляем.** Из детальных карточек ушёл (волна B), в листингах остаётся | `selectSql: wl.*` намеренно берёт звёздочку — так листинг не ломается при добавлении колонки. Ради ~100 байт бинарного шума на строку менять это на перечисление колонок невыгодно: цена — регулярные поломки при эволюции схемы |
| ~~**Новое №5**~~ ✅ (`MIGRATE_WIRING_ENABLED=false`) | **Ветка убрана 13.08.2026** (PR #199): шаг миграций в `update-production.sh` безусловен, тест `deployWiring.test.js` переписан на закрепление отсутствия флага | После baseline это foot-gun: экспорт переменной молча пропустит миграции перед подменой образа. Легитимного применения не осталось — откат кода без схемы идёт своим путём в скрипте. Не делаю здесь, потому что это правка боевого деплой-скрипта, и ей место в отдельном изменении с собственной проверкой, а не хвостом большой сессии |

### Эксплуатация — за вами (нужен sudo на .105)

| Что | Команда / место |
|---|---|
| `shutdown-timeout: 120` в `/etc/docker/daemon.json` | `runbooks/deployment.md` §2б, проверить `dockerd --validate` |
| Отключить спящую учётку `generator` | `UPDATE users SET is_active=false WHERE username='generator'` |
| Чистка ufw: v6-only правила 1883/1884, `51820/tcp`, дубль 443 | подготовлю точный список `ufw delete` по слову |
| **SEC-34j** на .105 | `sshd -T \| grep -E 'passwordauthentication\|permitrootlogin'`. На profk уже проверено: обе `no` |

---

## P3 — по касанию

### Аудит: сопровождение и упрощение (этапы 6–7)

| ID (серьёзность) | Что не так | Доказательство | Труд |
|---|---|---|---|
| ~~**A-18**~~ ✅ (P2) | ~~Валидатор принимает `1`/uppercase/с пробелами, а потребители разбирают по-разному~~ — **закрыто 18.09.2026** (PR #245; строка бэклога отстала от кода на день — поправлено 18.09). Единый `src/utils/envFlags.js`. Разборов одного по смыслу флага оказалось ПЯТЬ, а не три: к трём из отчёта добавились `ALERT_VERIFICATION_ENABLED` (своя, четвёртая форма) и `UK_USE_WEBHOOK_SENDER` в двух местах — пятый разбирал верно и сведён к общему парсеру не как исправление, а чтобы шестого не завелось. Парсер намеренно НЕ трактует неопознанное значение как «включено»: иначе опечатка `ture` открывала бы аварийный клапан `AUTH_BLACKLIST_FAIL_OPEN`. Попутно найден тест, закреплявший дефект как правильное поведение («`SECURE_COOKIES` values other than "true" leave secure off», с пояснением в комментарии, что `1` — «не каноническая строка») — та же форма, что у A-16; инвертирован, верная половина прежнего намерения сохранена отдельным тестом | `src/utils/envFlags.js`, `tests/jest/unit/envFlags.test.js` | S |
| ~~**A-19**~~ ✅ (P2) | ~~Console-транспорт печатает только timestamp/level/message~~ — **закрыто 18.09.2026** (PR #247): режима теперь два — в `LOG_CONSOLE_ONLY` (контейнер, 12-factor) в stdout идёт чистый JSON для сборщика, в обычном сохранена читаемая строка, но метаданные дописываются хвостом, а стек — отдельной строкой. Формат вынесен в `loggerConsoleFormat.js` не ради чистоты: внутри логгера его можно было проверить только перехватом `process.stdout.write`, и такой тест разваливался, стоило запустить рядом ЛЮБОЙ другой файл (4 из 4 падали — поток держит сам jest). Попутно: `colorize` стоял безусловно, то есть escape-коды цвета уезжали и в файл, и в сборщик; теперь цвет привязан к TTY | `tests/jest/unit/loggerConsoleMetadata.test.js`; мутация — возврат прежнего формата — роняет 3 из 4 | — |
| ~~**A-20**~~ ✅ (P2) | ~~CodeQL глобально исключает CSRF-запрос с устаревшим обоснованием~~ — **снято 18.09.2026** (PR #248). Обоснование звучало «API Bearer-only, куки только для debug-эндпоинтов»; с [P1-2] токены живут в HttpOnly-куках и middleware читает их оттуда — то есть браузер снова шлёт учётные данные автоматически, ровно условие CSRF. Защита (Origin-guard, SEC-23) на месте, но защита и АНАЛИЗ — разные вещи. Находки теперь видны; если какая-то окажется ложной, подавлять ПОШТУЧНО в Security-вкладке, а не возвращать глобальное исключение — это записано прямо в конфиге. Тест связывает три факта, которые разошлись молча: куки-аутентификацию, наличие guard'а и состав фильтров | `tests/jest/unit/codeqlCsrfCoverage.test.js` | — |
| ~~**A-21**~~ ✅ (P2) | ~~Ручной `backup-database.sh` выбирает ПЕРВЫЙ Postgres-контейнер на хосте~~ — **закрыто 17.09.2026** (PR #246). Насколько это близко к беде, видно на profk: там ЧЕТЫРЕ контейнера с `postgres` в имени, и первым идёт `uk-payment-postgres` — платёжная база другого проекта. Сегодня спасает лишь то, что фильтр по образу пока однозначен; это везение, а не устройство — достаточно сменить тег образа. Теперь контейнер спрашивается у СВОЕГО compose (`docker compose ps -q postgres`), а ненайденный — отказ, а не выбор наугад. Сам дамп делегирован `database/backup-cron.sh`: дубль уже разошёлся — у cron-варианта есть `--clean --if-exists --no-owner`, retention и выгрузка за пределы хоста, у ручного не было ничего из этого. Минус 87 строк | `tests/jest/unit/backupContainerResolution.test.js`; мутация — возврат поиска по хосту — роняет 2 теста | — |
| ~~**A-25**~~ ✅ (P3) | ~~`isAvailable`, `setFailureThreshold`, `setResetTimeout`, `createExternalServiceBreaker` не имеют production-потребителей~~ — **закрыто 18.09.2026**. Удалены четыре API и относящиеся к ним тесты (−30 runtime, −49 test строк, 0 зависимостей). Мера была ровно такой, как в отчёте: потребителей нет, а покрытие держало их живыми на вид. В шапке файла записано, почему их нет и при каком условии такой метод может вернуться: вместе с вызовом, а не раньше — правка порога на лету у breaker'а, который никто не настраивает, это не расширяемость, а вторая копия конфигурации | `src/utils/circuitBreaker.js`, `tests/jest/unit/circuitBreaker.test.js` (30 тестов зелёные после удаления) | S |
| ~~**A-26**~~ ✅ (P3) | ~~README описывает строгую цепочку Controllers→Services→Models, но DB-хелперы вызываются прямо из admin-контроллеров, SQL живёт и в сервисах, а модель `User` зависит от кэш-сервиса~~ — **закрыто 18.09.2026**. Repository-слой НЕ вводился — он дороже проблемы. Вместо этого цепочка названа тем, чем является (направление зависимостей, а не запрет), и зафиксированы три отступления с владельцами: восемь админ-контроллеров через единый `adminQueryBuilder` (восемь моделей-обёрток дали бы восемь мест расхождения фильтров, а не слой); агрегирующий SQL в сервисах (запрос принадлежит сценарию, а не сущности); `User` → `cacheService` (инвалидация обязана идти в той же операции, что и запись). Отдельно сказано, чем отступления НЕ являются, — иначе список читается как разрешение. Взятие соединения ради `pg_try_advisory_lock` к данным не относится: это управление сессией | `README.md`, `tests/jest/unit/architectureClaims.test.js` — тест связывает обе половины: каждое отступление должно быть названо в README И по-прежнему верно в коде, иначе документ разойдётся с кодом ровно так же молча, как разошлась исходная схема | S |


### Дешёвое, десять минут на пункт

| ID | Что | Труд |
|---|---|---|
| ~~**CO-8**~~ ✅ | ~~Делегаторы без единого вызова и без spy~~ — **оказалось уже сделанным** (обнаружено 14.08.2026): делегаторы удалены, на их месте поясняющий комментарий `alertService.js:879-885`; строка бэклога пережила фикс | — |
| ~~**DE-8**~~ ✅ | ~~`login.js`, `theme-toggle.js`, `api-config.js` подключаются в HTML сырыми~~ — **закрыто 14.08.2026**: в `ENTRIES` esbuild они уже были (dist собирался и выкатывался), сырыми оставались только `<script src>` в HTML — все пять ссылок (login/index/documentation/about) переведены на `public/dist/*.js?v=20260814` | — |
| ~~**R2-34**~~ ✅ | ~~Ссылка на удалённый `PowerTransformer.js` жива в `PROJECT_CONTEXT.md:57`~~ — **снято 13.08.2026**: упоминания в `PROJECT_CONTEXT.md` больше нет | — |
| ~~**комментарий к `sendSuccess`**~~ ✅ | ~~У воскрешённого `sendSuccess` ни слова об отмене «RAW success»~~ — **оказалось уже сделанным** (обнаружено 14.08.2026): развёрнутое обоснование отмены PR-3 стоит в `apiResponse.js:5-17`; шапки «очередь переехала в BACKLOG.md» в `AUDIT_REPORT.md` и `sprint-11-backlog.md` тоже давно стоят — волна A целиком закрыта | — |
| ~~**`docs/internal/tasks.md`**~~ ✅ | ~~Файл от 19.10.2025 требует «немедленно чинить SQL-инъекции»~~ — **снято 13.08.2026**: файл уже удалён | — |

### Дублирование и мёртвое

| ID | Что |
|---|---|
| ~~**CO-10**, остаток~~ ✅ | ~~Валидация координат в двух независимых копиях~~ — **закрыто 14.08.2026**: `CoordinateEditor.validateCoordinates` делегирует в `coordValidation.js`; попутно на общий валидатор переведена синхронизация маркера с картой (там стоял свой `isNaN`, и карта прыгала на широту 500, пока пользователь дописывал число). Отсутствие валидатора = отказ, не пропуск. `coordinateEditor.sharedValidation.test.js`. **Про токены темы — не дубль**: четыре обёртки (`script.js:19`, `admin.js:12`, `map-layers-control.js:12`, `powerUtils.js` `PU_T`) — это null-guard на случай незагруженного `BrandTokens`, сам fallback уже внутри `BrandTokens.token`. Дедуп потребовал бы зависимости от модуля, от отсутствия которого guard и защищает — оставлено осознанно |
| ~~**AUD-034**~~ ✅ | ~~Два байт-идентичных catch-блока в `alertForwarder.js`~~ — **закрыто 14.08.2026**: тело вынесено в общий `recordForwardingFailure`; рубеж — `alertForwarder.failureRecord.test.js` (поведение + SQL-литерал в файле ровно один раз) |
| ~~**Новое №3**~~ ✅ | ~~Мёртвый маппинг + admin-форма без guard'а от двойного сабмита~~ — **закрыто 14.08.2026**: маппинг `infrastructure-line(s)` удалён, `save()` получил `_saving`-guard с finally-сбросом; `coordinateEditor.hardening.test.js` |
| ~~**DE-7**~~ ✅ | ~~Около трети публичной API-поверхности дёргают только bash-смоки: `analyticsRoutes` — 13 эндпоинтов, фронт вызывает один~~ — **закрыто 18.09.2026**: все тринадцать покрыты в СОБРАННОМ роутере (`tests/jest/integration/analyticsRoutes.integration.test.js`, 36 проверок). Юнит-тесты на контроллер и сервис были и раньше, но они зовут функции напрямую — всё между маршрутом и функцией ими не проверялось: default-deny, `isAdmin` на четырёх изменяющих (его пропуск — повышение привилегий, а юнит остаётся зелёным), `validateIntParam` на трёх `:transformerId` и, главное, КУДА попадает путь. `/transformers/overloaded`, `/transformers/search` и `/transformers/statistics` объявлены ПОСЛЕ маршрутов с параметром: сегодня коллизии нет, но появись однасегментная `:transformerId`-ветка — слово ушло бы в неё. Мутации подтверждают рубеж: снятие `isAdmin` и снятие `validateIntParam` роняют ровно свои тесты. Попутно: имена методов сервиса местами расходятся с именами маршрутов, и `getLoadAnalysByZone` — опечатка, живущая в публичном методе (тест берёт имена из контроллера, а не придумывает, иначе заглушка молча не вызывалась бы) | `tests/jest/integration/analyticsRoutes.integration.test.js` | M |
| ~~**INIT-SCHEMA-PT**~~ ✅ | ~~Канонический init создаёт таблицу, которую 037 дропает~~ — **разобрано 12.09.2026, и «очевидная» починка оказалась ловушкой**: убрать `power_transformers` из init НЕЛЬЗЯ. Миграция 036 переносит из неё строки запросом `FROM power_transformers` без защиты от отсутствия таблицы, а миграции неизменяемы — удаление дало бы на свежем bootstrap «relation does not exist» и потерю четырёх засеянных трансформаторов (Фараби-1/2, Олмазор-1, Шифонур-1). Вместо удаления связь объяснена в обоих init-файлах и закреплена тестом `initSchemaPowerTransformers.test.js` (проверено: при удалении таблицы из init он падает) |
| **R2-35 остаток** | `trash/` (58 МБ) не удалён, а загнан в `.gitignore`. На обоих продах отсутствует — то есть это только локальная проблема |

### Отказоустойчивость (реальная, но без нагрузки)

| ID | Что |
|---|---|
| ~~**AR-5**~~ ✅ | ~~`OPEN→HALF_OPEN` не ограничивает пробы~~ — **закрыто 14.08.2026**: `halfOpenMaxProbes` (по умолчанию 3 — столько успехов требует закрытие), лишние вызовы идут в fallback как при OPEN; `circuitBreaker.halfOpenProbes.test.js` |
| ~~**AR-16**~~ ✅ | ~~`_resolveVerifying` берёт блокирующий `pg_advisory_lock`~~ — **закрыто 14.08.2026**: `_acquireVerifyLock` — try-лок 15×200 мс (параметры инжектируются тестами), не дождались → `VERIFY_LOCK_BUSY`, контроллер отвечает 503 «повторите» вместо 500/зависшего соединения |
| ~~**AR-20**~~ ✅ | ~~кэш принимает fallback с `load_percent: 0`~~ — **закрыто 14.08.2026**: `setTransformerAnalytics` вызывается только при `!data.is_fallback`; `analyticsService.fallbackCache.test.js` |
| **AR-13** | 12 ленивых `require` внутри функций кодируют неявную зависимость от порядка инициализации |
| ~~**AR-15**~~ ✅ | ~~`migrate-discover.js` не запрещает дубли номера~~ — **закрыто 14.08.2026**: `assertUniqueNumbers` в discovery — новый дубль NNN отклоняется; историческая пара 012 заморожена как единственное исключение (ровно эти два имени) |
| **B-011** | Митигация зафиксирована (app вне `uk-network`), корень — нет: nginx сидит в обеих сетях и проксирует по родовому имени `app:3000` |

### Тесты и CI

| ID | Что |
|---|---|
| **CO-4** | Порог покрытия 80% применяется только к `src/**`. `admin.js` и `script.js` (6228 строк вместе) вне покрытия и без гейта (`package.json:92-95`) |
| **CO-5**, остаток | ~~Пагинация непроверяема в принципе~~ — **основное закрыто 13.09.2026**: `buildPaginatedList` (общий для восьми admin-контроллеров) покрыт на живом Postgres — `tests/jest/db/adminQueryBuilder.db.test.js`, 15 тестов. Проверено мутациями точечно: снятие экранирования `%` роняет тест M-8, потеря половины OR в поиске — тест поиска, `totalPages` без `Math.max(1,…)` — тест пустой выборки, `offset: 0` — ровно два теста, зависящих от страницы. Попутно зафиксирована граница конструкции: фильтровать по колонке из JOIN нельзя, WHERE попадает в оба запроса, а COUNT строится только по базовой таблице. **Остаток:** сам `dbMock` по-прежнему матчит по подстрокам, и всё, что ходит в БД мимо `buildPaginatedList`, проверяется им же — выносить в `tests/jest/db/` по мере касания, а не разом |
| ~~**CO-11**~~ ✅ | ~~`p1-3-csp-sri.test.js` проверяет текст конфига регулярками, не поднимая ни nginx, ни helmet — битый конфиг с конфликтующими `add_header` не отличит~~ — **закрыто 18.09.2026, и претензия оказалась НЕ теоретической.** Измерено на живом периметре (ответы 200): `/css/style.css`, `/public/dist/theme-toggle.js`, `/brand/brand.css`, `/brand/favicon.svg` приходили ТОЛЬКО с `x-content-type-options` — без CSP, HSTS, X-Frame-Options и Referrer-Policy; `/api/*` шёл без `Permissions-Policy`; `https://assets.profk.uz` — вовсе без HSTS (апстримную снимает `proxy_hide_header`, своей не ставили). Причина прямо в конфиге и прямо в нём описана: комментарии «[R2-17] re-emit nosniff — the add_header above drops the server-level…» стоят в шести местах, но переизлучили в них ОДИН заголовок вместо набора. Прежний тест увидеть этого не мог: строки в файле есть, просто не в тех блоках. Существеннее прочего — SVG: это документ, и по прямой ссылке он исполнялся в нашем origin без CSP. Сделано: канонический набор вынесен в `nginx-config/security-headers.{infrasafe,profk}.conf` и подключается `include`'ом в шесть статических location'ов и в html-локацию (там он заменил развёрнутую копию с припиской «keep this in sync» — ровно такие приписки и расходятся); `/api/` получил недостающую `Permissions-Policy` (остальные четыре там от helmet — дубль дал бы два разных CSP в одном ответе); вхост `assets.profk.uz` получил свою HSTS. Отсутствующий файл роняет `nginx -t`, поэтому молча обнулить набор конструкция не может. | `tests/jest/unit/nginxHeaderInheritance.test.js` + `tests/jest/helpers/nginxConfig.js`. Тест моделирует ОДНО правило nginx (любой `add_header` в location отменяет наследование) и честно не знает правил ВЫБОРА location — поэтому его вывод сверен с живым периметром. Проверка сильнее модели: обе конфигурации подняты настоящим nginx в контейнере — `nginx -t` проходит, а `/css/style.css`, `/brand/mark.svg` и `/login.html` отдают 7 заголовков из 7 без дублей (было: 1). Мутация (возврат одной локации к одному nosniff) роняет ровно свой тест. **Правка НЕ выкачена:** конфиг периметра едет отдельно — `git pull` + `nginx -t` на целевом хосте + `reload` | S |
| ~~**EN-8**~~ ✅ | ~~18 actions на подвижных тегах, `generator/` не линтуется, `concurrency` только в E2E~~ — **закрыто 14.08.2026**: все 18 запинены на SHA с комментарием версии (формат уже был — у `trivy-action`); `concurrency` добавлен в оставшиеся три workflow (отменяются только PR-прогоны: на main `ci.yml` публикует образ в GHCR, и обрыв оставил бы прод без тега); `generator/` снят из `ignores`, получил ESM-блок конфига и вошёл в `npm run lint`. Рубеж — `workflowsPinned.test.js`: новый action без SHA или без комментария версии, как и workflow без `concurrency`, роняет сборку |
| ~~**EN-9**~~ ✅ | ~~Глобальный exclude `js/missing-token-validation`~~ — **снято 13.08.2026**: exclude в репозитории больше не существует (грепом не находится ни в workflows, ни в конфигах) |
| **плавающий тест** | `integrationRoutesCoverage › returns 500 when findById throws` — два падения примерно за 20 полных прогонов, всегда по таймауту, в одиночку проходит всегда. *12.09.2026: воспроизвести НЕ удалось — 3 полных прогона на `--maxWorkers=100%` и ещё ~8 обычных прогонов чистые. Простые причины исключены: логгер в сьюте замокан, лимитеров и таймеров в `integrationRoutes` нет. Спекулятивно поднимать таймаут не стали — это замаскировало бы причину, а не устранило* |
| **плавающий тест №2** | `entityRoutesValidation › POST /api/cold-water-sources › полное тело проходит` — **наблюдение 13.09.2026**: одно падение в полном прогоне `tests/jest/unit`, дальше 9 полных прогонов и 8 изолированных прогонов файла чистые. Механизм НЕ установлен: детали того падения не сохранились, воспроизвести не удалось. Форма совпадает с закрытым FLAKE-1 (supertest под поделённым между jest-воркерами CPU), но свидетельства этому нет, поэтому таймаут не поднимали — это замаскировало бы причину, а не устранило. Если поймается ещё раз — сохранять вывод целиком |
| ~~**плавающий тест**~~ ✅ **диагностировано 17.09.2026** | Улика наконец поймана — инструментом, а не вниманием (`--json --outputFile`). И она опровергает саму формулировку: это НЕ «один плавающий тест». Три прогона подряд на одном коммите дали **0, 19 и 6** падений, каждый раз в РАЗНЫХ сьютах (`alertServiceCoverage`, `alertVerificationService`, `totpService`), среди причин — прямые `Exceeded timeout of 5000 ms`. Опыт на той же машине, той же нагрузке и том же коммите: **параллельно 19 и 6 падений, `--runInBand` ноль из 3292**. Значит, причина — голодание по процессору под воркерами jest, а три прежних «наблюдения» были мягким краем того же явления.

**Поправка, сделанная в тот же день.** Вывод «последовательно — ноль» был сделан по ОДНОМУ прогону и оказался сильнее доказательства. Замер на пяти последовательных прогонах дал **0, 2, 0, 0, 1** при нагрузке, доросшей до LA ≈ 22. Каждый раз падал ДРУГОЙ тест (в последнем — `[AR-4] лимитер → канон`). Правильная формулировка: `--runInBand` заметно ослабляет нестабильность, но НЕ устраняет её; на сильно загруженной машине единичное падение вообще не является свидетельством дефекта. Проверять — повтором с `--json --outputFile` и взглядом на `uptime`, а не рассуждением. Это ровно гипотеза, записанная при закрытии FLAKE-1, — теперь со свидетельством. **Поднимать таймауты по-прежнему не следует**: это замаскировало бы ровно то, что мы только что научились видеть. Практическое правило: прежде чем поверить падению полного прогона на загруженной машине, повторить с `--runInBand` |

### Гигиена и периметр

| ID | Что |
|---|---|
| ~~**CO-6**~~ ✅ | ~~`createOverloadWarning` вставляет `objectName` без экранирования~~ — **закрыто 14.08.2026**: `PU_escape` на месте вставки (имя редактируется в админке — было бы stored-XSS в попапе); `powerUtils.escape.test.js` |
| **M-21** | CSP `img-src 'self' data: https:` остался на `/uk/`-локации обоих эджей (наши блоки сужены до tile-серверов). Это чужая SPA — сузить или зафиксировать осознанно |
| ~~**M-18**~~ ✅ | ~~Dev-Postgres пишет значения параметров в docker-логи~~ — **закрыто 14.08.2026**: `log_statement=all` убран, порог `log_min_duration_statement=250` (диагностика медленных запросов остаётся) |
| ~~**L-1**~~ ✅ | ~~Один IP-бакет на шесть auth-эндпоинтов~~ — **закрыто 14.08.2026**: refresh — свой бакет 60/15мин (`RATE_LIMIT_REFRESH_MAX`), 2FA-шаги — свой (`RATE_LIMIT_2FA_MAX`, наследует `RATE_LIMIT_AUTH_MAX` для E2E), disable-2fa — узкий 5/15мин с userId в ключе; `authRateLimitBuckets.test.js` |
| **AR-14** | 15 bind-обёрток и три `Object.defineProperty` в `ukIntegrationService.js` существуют ради тестов — продовый код несёт тестовую связность. *Было 26, стало 15* |
| **EN-7** | Нет Prettier, `.editorconfig`, `.nvmrc`; версия `1.0.1` заморожена, релизных тегов и CHANGELOG нет — «что задеплоено» восстанавливается только по SHA |

### Эксплуатация, мелочи

| Что | Где |
|---|---|
| Шум healthcheck'а mosquitto — около 2800 строк в сутки от собственной анонимной пробы | .105 |
| Ротация `integration_log` — на profk таблица начала расти по-настоящему | оба |
| Контейнеры mosquitto и wireguard работают, хотя в compose закомментированы | .105 |
| 31 висячий том, 945 МБ. Не чистить вслепую: на profk есть именованный `uk_media_uploads` | .105 |
| Алерт `SwapInUse` P2 с 7 августа — 446 МБ при пороге 256. Занимают python и uvicorn, то есть сервисы УК, не InfraSafe | .105 |
| `AccessBarrierQueueStale` P2 — возраст команды шлагбаума 41 день. Владелец — сторона УК (`B-006`) | .105 |

---

## P4 — заморожено до условия

| ID (алиасы) | Условие разморозки |
|---|---|
| **AR-8** = `M-2` = остаток `B-003` | Появление второй реплики. Cooldown `lastChecks` и анти-реплей `usedCodes` — per-process Map; при одной реплике не эксплуатируется |
| **AR-6** | Рост нагрузки. Потолок — 30 событий в минуту у outbox и 4 у верификации; факт на profk — 25 событий за трое суток |
| **L-4** = остаток `SEC-34e` | CSS-рефактор примерно 62 инлайн-стилей в popup'ах. Пока `style` в `ALLOWED_ATTR` DOMPurify в трёх местах, а CSP тоже разрешает inline — второго рубежа нет |
| **R2-12** | Осознанный defer: дублирование оркестрации 2FA между `login.js` и `script.js` оставлено, чтобы не вернуть баг порядка условий |
| **B-004** | Появление модульного графа. `bundle: false` — это минификатор, дробление раньше лишь размножит глобалы. **Дрейфует:** `admin.js` 3868 → 3982 → **3982**, `script.js` 2190 → 2246 → **2262** (замер 13.08) при пороге 4500. Единственная метрика беклога, которая ухудшается сама |
| **B-008** | `feature/frontend-redesign` не двигалась с 28 марта. Предусловие `B-004` тоже заморожено |

---

## Снято — закрыто, но в источниках не отмечено

Проверено 10 августа против `main`.

| ID | Чем закрыт | Проверка |
|---|---|---|
| **CO-9** | AR-4, волна 3 | `sendSuccess`/`sendCreated` — 8 настоящих вызовов вместо нуля |
| **R2-05** | AR-4, волна 3 | `success: false` в `analyticsController` и `middleware/auth.js` не встречается |
| **CVE-остаток** | EN-3, PR #180 (мажор eslint был единственным путём) | `npm audit` = 0 по всему дереву, включая dev |
| **L-5** | — | `nginx.production.conf:688` проксирует `/health` на апстрим, а не отдаёт статический 200 |
| **R2-35**, половина | — | Корневых `test_*.sh` в индексе не осталось; `trash/` на обоих продах отсутствует |
| **Новое №6** | R2-23 | В `alertController` типизированный код вместо подстроки русского текста |
| **SEC-34j** на profk | — | `permitrootlogin no`, `passwordauthentication no` |
| **AR-4**, **AR-10**, **AR-12**, **AR-3(а)**, **EN-3** | волны 3 и 4 | Закрыты 7 августа, в `AUDIT_REPORT.md` не отмечены — он правился шестого |
| **AR-2**, **SE-1** | код был готов, ждали оператора — шаги выполнены | `up{job="infrasafe_app"}` = 1 на обеих; `assets.profk.uz` → 200, старый путь → 301 |

Дополнено сверкой 13.08.2026 (против `fb92e777`):

| ID | Чем закрыт | Проверка |
|---|---|---|
| **Защита `main`** | OPS-003, PR #196 (12.08 вечером) | прямой пуш отклонён сервером с `enforce_admins`; 7 обязательных проверок |
| **R2-34** | попутно одной из волн | `grep PowerTransformer PROJECT_CONTEXT.md` → 0 |
| **`docs/internal/tasks.md`** | файл удалён | `ls` → нет |
| **EN-9** | exclude исчез вместе с конфигом | `grep -r missing-token-validation` → пусто по всему репозиторию |

Заодно: `infrasafe_circuit_breaker_opened_total` пуст. Счётчик с меткой не появляется,
пока не сработает — значит после фикса AR-1 предохранитель не открывался ни разу.

---

## Контекст: что вообще под нагрузкой

Беклог взвешивался по риску в коде. Вот что на площадках на 10 августа:

| | profk | .105 |
|---|---|---|
| здания | 30 | 8 |
| трансформаторы | **0** | 4 |
| контроллеры | **0** | 2 |
| метрики | таблица пуста | нет с 16 июня |
| события УК | 25 за 3 суток, все `success` | нет с 23 июля |

На главной площадке живёт только интеграция с УК. Энергетическая часть —
представление, аналитика нагрузки, проверки напряжения, определение offline —
обслуживает пустой набор данных. Четыре пункта (`AR-6`, `AR-8`, `AR-20`, `DE-7`) описывают код, который сегодня
не может сработать по отсутствию входных данных.

`AR-21` и `AR-17` были в этом же списке и от отсутствия нагрузки становились не
менее срочными, а более — они привязаны к дате подключения железа. Оба закрыты
волной E: первый кодом, второй замером, показавшим, что проблемы нет.

---

## План реализации

### ✅ Волна A — сведение и дешёвая уборка (закрыта 14.08.2026)

Планировался один PR (`CO-8`, `DE-8`, `R2-34`, комментарий к `sendSuccess`,
удаление `docs/internal/tasks.md`, шапки в `AUDIT_REPORT.md` и
`sprint-11-backlog.md`), но при исполнении выяснилось, что 5 из 6 пунктов уже
закрыты прошлыми волнами — жили только строки бэклога. Реальной работы осталась
одна: DE-8 (пять `<script src>` переведены на `public/dist`).

### Волна B — admin-слой

`AR-3(б)` + `Новое №8` **одним PR**, порядок внутри жёсткий: сперва разбор статуса
в catch, затем перевод на модели. Иначе типизированные 4xx моделей окажутся
проглочены. Попутно снимается `AR-3(в)` — `adminQueryBuilder` остаётся только под
листинги. Закрывает три ID из двух источников сразу.

### ✅ Волна C — валидация, пачка S-размера (PR #186)

`CO-7`, `L-6`, `L-7` закрыты; `AR-11` — половина (`retryLog`). Общая тема:
вход не проверялся там, где должен, и отказ приходил как 500 вместо 4xx.
Правильные образцы для всех четырёх нашлись в самом репозитории — это был
перенос дисциплины, а не изобретение.

Побочно: whitelist статуса контроллера поставлен на ВСЕ ТРИ пути записи, а не
только на `create` (приём M-12), и модель перестала глушить собственную 400 в
500. Найден счётчик `active_controllers_count`, который всегда ноль — заведён
отдельным пунктом со сроком.

### ✅ Волна D — гарантии окружения (PR #187)

`AR-9` + `M-7`/`M-11` + `EN-6` закрыты одним узлом, как и планировалось: разносить
их было бы бессмысленно — это три симптома одного пробела.

### Волна E — до приезда железа

`AR-21` и `AR-17`. Срок задаёт не беклог, а ADR #138: обе дыры проявятся в первый
день работы телеметрии. Планировать по факту решения по ADR.

### Волна F — сессионный слой

**Волна F закрыта 12.08.2026** — `M-5` → `M-6` → `M-4` в этом порядке.

Задним числом о порядке: `M-6` планировался как поставщик примитива `token_version`
для `M-4`. Не понадобился — `M-6` решился timestamp-рубежом поверх уже
существовавшего cutoff'а, а `M-4` не нуждался ни в каком новом примитиве. Связь
между пунктами оказалась мнимой; порядок при этом всё равно был удачным, потому
что `M-5` и `M-6` трогают тот же слой, что и `M-4`, и делать их вперемешку было
бы дороже. Остался `M-4-contract` — снятие поля через выкладку.

### Волна G — тестовая инфраструктура

`R2-24` ✅ и `R2-14` ✅ сделаны. `STAGING-VM` + `Новое №4` отложены решением
владельца (11.08.2026). Пока staging'а нет, каждый деплой идёт сразу в оба
прода — это фон для всего остального в списке.

~~**Новое, найдено 12.08.2026: у `main` нет защиты ветки вообще**~~ —
**закрыто вечером того же дня (OPS-003, PR #196)**: защита включена с
`enforce_admins` и СЕМЬЮ обязательными проверками (`Lint`, `Tests & Coverage`,
`E2E (Docker stack)`, `Secret scan (gitleaks)`, `npm audit`, `Docker image`,
`Analyze JavaScript/TypeScript`) — больше минимального набора из четырёх,
который здесь предлагался. Плюс локальные хуки `.githooks/` (pre-push,
commit-msg) через `postinstall` и профиль выкатки из `.deploy-env` без
умолчания. Enforcement проверен попыткой прямого пуша: отклонён и хуком,
и сервером, с админскими правами включительно.

Боевое подтверждение R2-14 + защиты — 13.08 на PR #183: E2E отработал за
1m38s, PR не мог смержиться, пока проверка не появилась. Попутная gotcha
процесса: **PR, созданный ДО добавления новой обязательной проверки, не
смержится никогда** — проверка на нём не запускается сама, нужен свежий пуш
в ветку (для dependabot — комментарий `@dependabot rebase`).

### Волна H — аудит 08.09.2026

Порядок — из дорожной карты отчёта (раздел 8), она упорядочена по зависимостям,
а не по серьёзности.

1. ~~**Доступ.**~~ ✅ **выполнено 17.09.2026** (PR #226): `A-11` → `A-01`
   вместе с `A-16` в части TOTP-теста → CLI сброса → `A-02` с миграцией 044.
   Порядок внутри PR был обязателен: между `A-11` и `A-01` существует
   состояние «обход закрыт, восстановления нет». `A-12` закрыт следом
   (PR #231) — блок пуст.
2. ~~**Деплой.**~~ ✅ **выполнено 17.09.2026**: `A-09` + `A-13` + `A-14`
   (PR #227), `A-10` (PR #230), плюс PR #233 — дефект, найденный самой
   репетицией (откат возвращал ноль). Сценарий отказ → откат → повтор того же
   SHA исполнен ЧАСТИЧНО: ловушка на живой площадке подтверждена, содержательное
   восстановление — нет (подробности выше, в блоке P1). Остаток — один шаг
   подготовки площадки, требующий разрешения владельца.
3. ~~**Данные и показания.**~~ ✅ **выполнено 17.09.2026**: ~~`A-05`~~ (PR #236),
   ~~`A-06`~~ (PR #234), ~~`A-07`~~ (PR #235). ~~`A-08`~~ ✅ **закрыто**
   (PR #228) — отдельно от `A-07`, вопреки первоначальной связке: подмена
   имени поля и различение «данных нет» от «нормы» — разные правки, и
   ждать второй, держа первую, было незачем.
4. ~~**Интеграция.**~~ ✅ **выполнено 18.09.2026**: ~~`A-04`~~ (PR #238),
   ~~`A-03`~~ — обе половины (PR #237 — атомарность намерения и очереди;
   сверяющий проход — этот PR). Миграция, которую закладывали под отметку
   попыток, не понадобилась: отметка легла в существующий JSONB `data`.
5. ~~**Воспроизводимость.**~~ ✅ **выполнено 17.09.2026**: ~~`A-15`~~ (PR #244),
   ~~`A-17`~~ (PR #240), ~~`A-22`~~ (PR #242), ~~`A-23`~~ (PR #243), ~~`A-24`~~
   (PR #241).
6. ~~**Сопровождение.**~~ ✅ **выполнено 18.09.2026**: ~~`A-18`~~ (PR #245),
   ~~`A-19`~~ (PR #247), ~~`A-20`~~ (PR #248), ~~`A-21`~~ (PR #246).
7. ~~**Упрощение.**~~ ✅ **выполнено 18.09.2026**: ~~`A-25`~~, ~~`A-26`~~ — после закрытия поведенческих рисков, как и планировали.

**Связь с волной G.** Защита ветки, включённая 12.08, заметно снижает вес `A-14`
(см. строку находки): образ публикуется только при push в `main`, а туда ничего
не попадает мимо семи проверок. Это не отменяет находку, но переводит её из
«деплой может выкатить непроверенный код» в «остаётся узкое окно скоса мержей».

Окно закрыто 17.09: `needs` у `docker-image` виден в деле на прогоне `main`
`f12fb5e7` — образ начал собираться только после четырёх зелёных проверок.

**Чего в отчёте нет.** Аудит не запускал БД, миграции, Docker E2E и нагрузку, не
проверял боевые флаги, ротацию секретов и восстановление из бэкапа — это сказано
в его разделе 6 прямо. Поэтому «не найдено» в отчёте означает «не найдено на
прочитанной поверхности», а не «отсутствует».

### Новое, найдено при деплое 12.08.2026

| ID | Что | Доказательство | Труд |
|---|---|---|---|
| ~~**OPS-002**~~ ✅ **закрыто 13.08.2026 (PR #199)**: oid'ы из `git ls-tree` + один `git cat-file --batch` + сабкоманда `checksum-batch` — 42 контейнера свелись к одному; разбор потока по размеру, а не по строкам. Исходная формулировка ниже. | Деплой на profk занимал **минуты** на шаге `migrate up` даже когда применять нечего: на хосте нет node, и `file_checksum` поднимает ОТДЕЛЬНЫЙ контейнер `docker run … migrate-discover.js checksum` на КАЖДУЮ из 42 миграций. Само по себе не ломает ничего (деплой доходит успешно), но выглядит как зависание и растит окно, в котором фронт и бэкенд разъехались. Чинится подсчётом всех сумм за один запуск контейнера | `scripts/migrate.sh:148-149` (`file_checksum` в цикле по файлам); наблюдение: 42 последовательных `docker run` на profk | S |

### Новое, найдено 13.08.2026 (уборка контекста + курация CLAUDE.md)

Сделано вне беклога, PR #197: `CLAUDE.md` 60,9k → 22,5k байт — выводимое из
кода вырезано, миграции/тесты → вложенные `CLAUDE.md`, UK-интеграция / ENV /
рантайм → скиллы `.claude/skills/`; история — verbatim в
`docs/audit/CLAUDE-MD-ARCHIVE.md`, снапшот в `CLAUDE-MD-snapshots/`.
Проверка: 14/14 активных предупреждений сохранены, path-ссылки 0% потерь.

| ID | Что | Доказательство | Труд |
|---|---|---|---|
| **CLAUDE-MD-PROCESS** | Исходные 60k накопились процессом: каждый фикс дописывал датированный абзац прямо в `CLAUDE.md`. Без правила файл вернётся к прежнему весу за полгода. Предлагаемое правило (нужно решение владельца): «новые fix-заметки — в `docs/audit/`, в `CLAUDE.md` — одна строка со ссылкой». Вариант посильнее — CI-проверка на паттерн дат в аддициях к `CLAUDE.md` | рост файла 24k→61k за ~4 месяца по git-истории | XS + решение |
| **SKILL-DUP-RISK** | Вынос в скиллы создал ВТОРУЮ копию фактов (топология УК, роли БД, флаги ENV) рядом с кодом и вложенными `CLAUDE.md`. Пока копии свежие; при следующем изменении топологии или ENV-набора обновлять надо ОБА места — скилл и код. Указатели на источник истины есть в двух скиллах из трёх (`env-reference` → `src/config/env.js`, `infrasafe-runtime` → `update-production.sh`); у `uk-integration` его нет, и проверки дрейфа нет нигде | `.claude/skills/{uk-integration,env-reference,infrasafe-runtime}` | наблюдение, без действия |

### Вне волн

Решения владельца (`M-20`, `DE-6`, `Новое №5`, судьба staging) не требуют кода и
могут быть приняты в любой момент — они разблокируют соответствующие пункты.
Шаги с sudo на .105 независимы от всего.

### Чего делать не следует

Спекулятивный рефакторинг alert/UK-домена: ядро работает, покрыто тестами, уже
пережило один разбор (`AUD-012`). Дробление `admin.js`/`script.js` до появления
модульного графа. Разморозка `P4` без наступления условия — там нет ни одного
пункта, который проявляется при одной реплике и пустой телеметрии.
